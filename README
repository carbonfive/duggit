Dependencies:
* zlib: --with-zlib-dir=$rvm_path/usr
* openssl: --with-openssl-dir=/usr/local

Dependencies (native extensions):
* libxslt-dev (for nokogiri)
* libxml2-dev (for nokogiri)
* sqlite3
* libsqlite3-dev or sqlite3-devel
